jar {
    manifest {
        attributes 'Implementation-Title': 'CS361/Weiss Sorting Algorithms',
           'Implementation-Version': version,
            'Main-Class': 'edu.odu.cs.cs361.animations.CS361SortingAnimation'
    }
    from { configurations.runtimeClasspath.collect {
         it.isDirectory() ? it : zipTree(it) } }
	from sourceSets.main.java
}
